<script setup lang="ts">
import type { Country } from '../models/country.models';

    interface Props{
        countries: Country[];
    }
    defineProps<Props>();
</script>
<template>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-3">
        <div v-for="country in countries" class="border border-gray-300 rounded p-3">
            {{ country.name.common }}
        </div>
    </div>
</template>